import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wobblewingstudios.wordswoop',
  appName: 'WordSwoop',
  webDir: 'dist'
};

export default config;
